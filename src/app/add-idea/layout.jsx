export const metadata = {
  title: "Add New Idea",
  description: "Share your startup concept with the community.",
};

export default function AddIdeaLayout({ children }) {
  return <>{children}</>;
}
